---
const { class: className } = Astro.props;
---

<div class={`flex items-center ${className}`}>
  <span class="text-2xl font-bold text-primary-600">MM</span>
  <span class="ml-2 text-xl font-semibold text-gray-800 dark:text-white hidden sm:inline">Money Mastery</span>
</div>