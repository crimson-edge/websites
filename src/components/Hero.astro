---
import { Image } from 'astro:assets';
---

<section class="relative bg-gradient-to-b from-gray-900 to-gray-800 py-16 sm:py-24 lg:py-32">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center">
      <h1 class="text-3xl sm:text-4xl lg:text-5xl xl:text-6xl tracking-tight font-extrabold text-white">
        <span class="block mb-2">Master Your Money.</span>
        <span class="block text-primary-400">Secure Your Future.</span>
      </h1>
      <p class="mt-3 sm:mt-4 lg:mt-5 text-base sm:text-lg lg:text-xl max-w-md sm:max-w-2xl lg:max-w-3xl mx-auto text-gray-300">
        Access expert-curated financial resources, interactive calculators, and comprehensive guides to take control of your financial journey.
      </p>
      <div class="mt-5 sm:mt-8 lg:mt-10 max-w-sm sm:max-w-none mx-auto">
        <div class="grid gap-3 sm:gap-4 sm:grid-flow-row-dense sm:grid-cols-2 sm:max-w-xl lg:max-w-2xl mx-auto">
          <a
            href="#resources"
            class="flex items-center justify-center px-6 py-3 sm:px-8 border border-transparent text-base sm:text-lg font-medium rounded-md text-white bg-primary-600 hover:bg-primary-700 transition-colors duration-300 shadow-lg hover:shadow-xl"
          >
            Explore Resources
          </a>
          <a
            href="#calculators"
            class="flex items-center justify-center px-6 py-3 sm:px-8 border border-transparent text-base sm:text-lg font-medium rounded-md text-primary-600 bg-white hover:bg-gray-50 transition-colors duration-300 shadow-lg hover:shadow-xl"
          >
            Try Calculators
          </a>
        </div>
      </div>
    </div>
  </div>

  <!-- Mobile-optimized background pattern -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <div class="transform-gpu opacity-20">
      <div class="absolute inset-0 bg-gradient-to-b from-primary-500/20 to-transparent"></div>
      <svg
        class="absolute left-[calc(50%-20rem)] sm:left-[calc(50%-30rem)] top-1/2 transform -translate-y-1/2"
        width="100%"
        height="100%"
        viewBox="0 0 1000 1000"
        xmlns="http://www.w3.org/2000/svg"
      >
        <defs>
          <pattern id="grid" width="40" height="40" patternUnits="userSpaceOnUse">
            <path d="M0 40L40 0M0 0L40 40" stroke="currentColor" stroke-width="1" fill="none" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid)" />
      </svg>
    </div>
  </div>

  <!-- Schema.org structured data -->
  <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "WebSite",
      "name": "Money Mastery Resources",
      "description": "Expert financial resources, tools, and guidance for mastering your money and securing your financial future.",
      "url": "https://moneymasteryresources.com",
      "potentialAction": {
        "@type": "SearchAction",
        "target": "https://moneymasteryresources.com/search?q={search_term_string}",
        "query-input": "required name=search_term_string"
      }
    }
  </script>
</section>

<style>
  /* Smooth transitions */
  a {
    transition: all 0.3s ease;
  }

  /* Improved touch targets for mobile */
  @media (max-width: 640px) {
    a {
      min-height: 44px;
    }
  }

  /* Prevent text overflow */
  h1 {
    word-wrap: break-word;
    overflow-wrap: break-word;
    hyphens: auto;
  }

  /* Optimize background pattern for mobile */
  @media (max-width: 640px) {
    svg {
      opacity: 0.5;
      transform: scale(0.75) translateY(-50%);
    }
  }

  /* Improved button spacing on small screens */
  @media (max-width: 640px) {
    .grid {
      grid-template-columns: 1fr;
    }
  }
</style>
